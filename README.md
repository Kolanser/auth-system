# 🔐 Кастомная система аутентификации и авторизации на Django

Проект демонстрирует полную реализацию кастомной системы аутентификации и RBAC (Role-Based Access Control) авторизации на Django REST Framework.

## 🎯 Основные цели проекта

- Реализовать собственную систему аутентификации (без использования встроенных решений Django)
- Создать гибкую систему RBAC с использованием ContentType
- Показать понимание различий между аутентификацией и авторизацией
- Реализовать JWT токены с кастомной логикой


## ✨ Ключевые особенности

### 🔑 Кастомная аутентификация
- Регистрация с email/паролем (bcrypt хеширование)
- JWT токены с 15-минутным сроком жизни
- Мягкое удаление пользователей (soft delete)
- Выход из системы (logout)

### 🛡️ Продвинутая RBAC система
- Использование Django ContentType для связи с любыми моделями
- Детальные права: чтение своих/всех, создание, обновление своих/всех, удаление своих/всех
- Динамическая проверка прав через кастомные permissions
- Административный интерфейс для управления правами

📡 API Endpoints

🔐 Аутентификация

POST   /api/v1/accounts/registration/     # Регистрация
POST   /api/v1/accounts/login/        # Логин (получение JWT)
POST   /api/v1/accounts/logout/       # Выход
GET    /api/v1/accounts/profile/      # Профиль пользователя
PATCH  /api/v1/accounts/profile/      # Обновление профиля
DELETE /api/v1/accounts/profile/      # Мягкое удаление аккаунта

👥 Управление ролями и правами (только для администраторов)

GET    /api/v1/roles/                 # Список ролей
POST   /api/v1/roles/                 # Создание роли
GET    /api/v1/roles/{id}/            # Детали роли
PUT    /api/v1/roles/{id}/            # Обновление роли
DELETE /api/v1/roles/{id}/            # Удаление роли

GET    /api/v1/roles/access-rules/    # Список правил доступа
POST   /api/v1/roles/access-rules/    # Создание правила
GET    /api/v1/roles/access-rules/{id}/ # Детали правила
PUT    /api/v1/roles/access-rules/{id}/ # Обновление правила
DELETE /api/v1/roles/access-rules/{id}/ # Удаление правила

GET    /api/v1/roles/elements/ # Доступные модели для прав

🛒 Бизнес-логика (пример)

GET    /api/v1/products/              # Список товаров
POST   /api/v1/products/              # Создание товара (с проверкой прав)
GET    /api/v1/products/{id}/         # Детали товара
PUT    /api/v1/products/{id}/         # Обновление товара
DELETE /api/v1/products/{id}/         # Удаление товара

GET    /api/v1/orders/                # Список заказов
POST   /api/v1/orders/                # Создание заказа
GET    /api/v1/orders/{id}/           # Детали заказа
PUT    /api/v1/orders/{id}/           # Обновление заказа
DELETE /api/v1/orders/{id}/           # Удаление заказа


🧪 Тестирование
Для проверки работоспособности проекта написаны тесты

python manage.py test --parallel
